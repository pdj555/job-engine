{
  "name": "Job Engine",
  "install": "command -v uv >/dev/null 2>&1 || curl -LsSf https://astral.sh/uv/install.sh | sh; uv sync --extra dev && npm --prefix web ci",
  "terminals": [
    {
      "name": "api",
      "command": "uv run job-engine serve"
    },
    {
      "name": "web",
      "command": "cd web && npm run dev"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "web", "port": 3000 }
  ]
}
