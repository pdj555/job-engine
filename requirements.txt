# Runtime deps for the Vercel Python function (the serverless subset of
# pyproject.toml — no CLI-only packages like uvicorn/typer/rich).
fastapi>=0.115.0
openai>=1.50.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
